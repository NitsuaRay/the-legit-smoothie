import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors Extracted from Logo
  static const Color primary = Color(0xFF1E1E1E);        // Panda Black (Primary actions/text)
  static const Color primaryAccent = Color(0xFFA87EA9);  // Taro Purple (Panda Face Accent)
  static const Color secondary = Color(0xFFC19A6B);      // Milk Tea Tan (Boba Cup Body)
  static const Color secondaryDark = Color(0xFF8B5A2B);  // Boba Pearl Brown

  // Neutral Colors
  static const Color background = Color(0xFFFFFDF9);     // Soft Milk Cream
  static const Color surface = Color(0xFFFFFFFF);        // White Card Backgrounds
  static const Color textPrimary = Color(0xFF1E1E1E);    // Dark Text
  static const Color textSecondary = Color(0xFF757575);  // Subtitles & Muted Text
  static const Color border = Color(0xFFE0E0E0);         // Subtle Dividers
  
  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
}